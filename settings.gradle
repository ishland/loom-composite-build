pluginManagement {
	repositories {
		maven {
			name = 'Fabric'
			url = 'https://maven.fabricmc.net/'
		}
		mavenCentral()
		gradlePluginPortal()
	}
}

includeBuild('aIncludedBuild') {
	dependencySubstitution {
		substitute module('aIncludedBuild:aIncludedBuild') using project(':')
	}
}
